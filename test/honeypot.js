var assert = require('assert'),
    Honeypot = require('../lib/Honeypot');

describe('Honeypot', function () {
  describe('#join()', function () {
    it('should find new nodes', function (done) {
      done();
    });
  });
});
